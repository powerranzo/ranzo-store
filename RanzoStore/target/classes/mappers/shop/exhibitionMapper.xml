<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<!-- 다른 mapper와 중복되지 않도록 네임스페이스 기재 -->
<mapper namespace="exhibition">
	<select id="list" resultType="ExhibitionDTO">
		SELECT * FROM EXHIBITION_TB
	</select>
	
	<select id="detail" resultType="ExhibitionDTO">
		SELECT code, location, gallery, title, summary, thumnail, product_info, start_date, end_date, hit, sales FROM EXHIBITION_TB
		WHERE code = #{exhibitionCode}
	</select>

	<select id="getProductInfo" resultType="ProductInfoDTO">
		SELECT code, content, attach
		FROM PRODUCT_INFO_TB
		WHERE code = #{code}
	</select>

	<select id="getReserveInfo" resultType="ProductInfoDTO">
		SELECT code, attach, content FROM Product_Info_TB
	</select>
</mapper>














